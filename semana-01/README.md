# Semana 01: Clases y Objetos Básicos - Hotel Descanso Real

## 🏨 Dominio: Hotel Descanso Real

El objetivo de esta semana fue establecer las entidades fundamentales del sistema de gestión hotelera en Java.

## ⚙️ Archivos Entregados

* `Habitacion.java`
* `Huesped.java`
* `Main.java`

## 🔑 Conceptos Implementados

1.  **Clases:** Definición de `Habitacion` y `Huesped`.
2.  **Atributos:** Se definieron propiedades clave (`numero`, `precioBase`, `nombreCompleto`, `diasEstancia`).
3.  **Encapsulación Básica:** Uso de atributos `private` y definición de métodos de acceso (`getters` y `setters`).
4.  **Métodos de Negocio:** Implementación de la primera lógica básica:
    * `Habitacion.calcularPrecioConDescuento()`
    * `Huesped.registrarCheckIn()`

## 🚀 Ejecución

Se demostró la creación de instancias de objetos y la ejecución de sus métodos en `Main.java`.